{
  "name": "wura-grand-hotel",
  "version": "2.0.0",
  "private": true,
  "description": "Wura Grand Hotel — fullstack MERN booking app (MongoDB + Express + React + Node) with Vite + Tailwind CSS.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "MIT",
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}
